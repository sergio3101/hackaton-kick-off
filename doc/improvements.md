# Улучшения и баги — бэклог

Список наблюдений по ходу UAT/smoke-тестирования MVP.

**Приоритеты:**
- **P0** — блокер, без этого нельзя сдавать
- **P1** — заметно ухудшает впечатление, желательно успеть к демо
- **P2** — nice-to-have, без последствий для демо

**Статусы:** `[ ]` — открыт, `[~]` — в работе, `[x]` — сделано, `[-]` — не делаем (вынесено в out-of-scope)

---

## Баги

- [ ] **P1** **Повторы голосовых вопросов в одной сессии** — наблюдается, что один и тот же вопрос иногда задаётся дважды в рамках одной сессии. Проверить логику выбора в `routers/sessions.py:create_session` (random.choice по `by_topic` с `pool.remove(pick)` — должно гарантировать уникальность; возможно проблема в дублировании при коротком пуле или в follow-up). Воспроизвести на ≥3 темах × middle, посмотреть `session_questions.bank_id` после create.

## UX / UI

- [ ] **P1** **Переименование меню и устранение дублирования** — текущее меню «Дашборд / Новый проект / История / Аналитика» → «Дашборд / Проекты / Сессии / Аналитика». Конкретно: (1) сделать настоящий Dashboard (см. ниже), (2) текущий `Dashboard.tsx` (фактически список проектов) переименовать в `Projects.tsx` и подключить под `/projects`, (3) убрать «Новый проект» из меню — точка входа остаётся как кнопка «Загрузить новый» внутри Проектов, (4) `History.tsx` переименовать в `Sessions.tsx`, путь `/history` → `/sessions`, в меню — «Сессии».
- [ ] **P1** **Настоящий Dashboard как hub** — новая страница `/dashboard` с тремя зонами: (a) KPI-полоса (использовать существующий `GET /api/analytics/overview` — `total_sessions`, `overall_avg_score`, `total_questions_answered`); (b) если есть `status="active"` сессия — крупная CTA-карточка «Продолжить интервью»; (c) quick actions — «Создать сессию по последнему проекту», «Загрузить новый ТЗ», ссылка на полную аналитику. `useQuery` к `/api/sessions` + `/api/analytics/overview`. Закрывает запрос «вынеси аналитику на дашборд».
- [ ] **P1** **Статистика по проекту в карточке Projects** — на карточке проекта показать «N сессий · средний M% · последняя D дней назад». Бэк: расширить `RequirementsOut` или новый эндпоинт `GET /api/requirements/{id}/stats` (count sessions, avg verdict-score, last_session_at). Фронт: `Projects.tsx` (бывший Dashboard) показывает мини-метрику. Помогает выбрать, по какому проекту тренироваться.
- [ ] **P2** **Список сессий по проекту на странице деталей** — `RequirementsDetail.tsx` сейчас показывает summary/topics/bank без сессий. Добавить блок «Прошлые сессии по этому проекту» (фильтр `GET /api/sessions?requirements_id=X` — нужен query-параметр на бэке) + кнопка «Создать новую сессию». Делает страницу самодостаточной точкой входа в тренировку.
- [ ] **P2** **Из отчёта — кнопка «Ещё одну сессию по этому проекту»** — на `Report.tsx` рядом с «← К истории» и «Скачать PDF» добавить ссылку на `/requirements/{requirements_id}/new-session`. Поощряет повторную тренировку, закрывает loop «отчёт → следующая итерация».
- [ ] **P2** **Breadcrumbs на детальных страницах** — `Interview.tsx`, `Report.tsx`, `RequirementsDetail.tsx`, `NewSession.tsx` — компонент `<Breadcrumbs>` сверху в виде `Проекты / [Название проекта] / Сессия #42`. Контекст не теряется при глубокой навигации.
- [ ] **P2** **Onboarding для пустого аккаунта** — если у пользователя нет ни одного `Requirements` — на Dashboard показать пошаговый wizard «1. Загрузите .md или используйте шаблон → 2. Создайте сессию → 3. Получите отчёт» с CTA на каждый шаг. Активирует пользовательский flow в первый визит.

## Функциональность

- [ ] **P1** **Повтор вопроса голосом** — кнопка «Повторить вопрос» в `VoicePanel`, доступна в фазах listening/idle. На бэке новое WS-сообщение `{"type":"replay"}` — выполняет `synthesize_speech(item.prompt_text)` и шлёт `question` ещё раз с `is_replay=true`. Без перетранскрипции / повторной оценки. Полезно если кандидат прослушал. *(В Realtime-режиме закрыто естественно: можно попросить модель устно «повтори вопрос» — в legacy-режиме всё ещё актуально.)*
- [ ] **P2** **Текстовый режим в течение всей сессии** — сейчас текстовый ответ доступен per-question через toggle «Ответить текстом / кодом». Сделать опцию «Текстовое интервью» при создании сессии, при которой все вопросы автоматически открываются в текстовом редакторе, без TTS/STT. Переключатель в `NewSession.tsx`, флаг в `Session` (новое поле `mode: voice|text`), в `VoicePanel` — скрывать микрофон при `mode=text`.

## Голос (STT / TTS / push-to-talk)

*(Раздел в основном закрыт миграцией на Realtime API — см. блок «Готово».)*

## LLM-качество (вопросы, оценка, кодинг-ревью)

- [ ] **P2** **Fallback при невалидном LLM-ответе** — `json.loads(response.choices[0].message.content)` в `evaluate.py`/`extract.py`/`generate.py` падает при partial/обрезанном JSON; текущая обработка возвращает `eval_failed` для пользователя. Добавить retry с упрощённым промптом + явный fallback (`verdict="incorrect"` + rationale «Не удалось разобрать ответ LLM»). Лог-уровень WARNING с raw-ответом для диагностики.

## Производительность

- [ ] **P2** **cost_tracker: единая транзакция с основной сессией** — сейчас `_record()` в `app/llm/cost_tracker.py` намеренно открывает свой `SessionLocal` (best-effort, не падает основная логика). Минус: при rollback основной сессии запись об usage остаётся в БД (orphan). Перевести на параметр `db: Session` пробрасываемый из роутера; для WS-контекста — использовать существующий `db` из `interview_ws.py`. Не критично для хакатона.

## Деплой / операции

- [ ] **P2** **Логирование с ротацией** — `app/logging_setup.py` использует `logging.basicConfig()` без RotatingFileHandler. Для демо ОК (run в Docker, stdout → docker logs), но для долгоживущего staging-стенда нужен `RotatingFileHandler` с max 10 MB и 5 backups, либо явный stdout-only (12-factor app).
- [ ] **P2** **WeasyPrint CSS hardening** — в `app/reports/pdf.py:_verdict_badge` есть inline `style="background:{color}1a;color:{color};"`. `color` берётся из захардкоженного `VERDICT_COLOR` dict (safe сейчас), но fragile: если кто-то добавит user-controlled поле в style — будет CSS-injection. Заменить на `class="verdict-correct"` + статические правила в общем `CSS`, исключив возможность интерполяции пользовательских значений в style-атрибуты.

---

## Готово / решено

- [x] **P1** **Полноценное редактирование пользователей в `/admin/users`** — раньше inline можно было сменить только роль и активность; email/ФИО/пароль задавались только при создании. Что появилось:
  - **Backend**: `AdminUserPatch.email: EmailStr | None` (`backend/app/schemas.py`), в `update_user` (`backend/app/routers/admin.py`) — проверка уникальности нового email с 409 «Email уже используется». Гарды на самозаблок (нельзя снять себе admin / деактивировать / удалить себя) сохранены. Миграция БД не требуется.
  - **Frontend**: в `AdminUsers.tsx` появилась кнопка `Icon name="edit"` рядом с trash; клик раскрывает аккордеон-форму под строкой с полями `Email / ФИО / Новый пароль` (плейсхолдер «оставьте пустым, чтобы не менять»). Сабмит шлёт частичный PATCH (только изменённые поля), пустой пароль не отправляется. Локальный inline-error пишется из `detail` ответа — при 409 аккордеон не закрывается. Добавлен тип `UserPatch` в `frontend/src/api/types.ts`.
- [x] **P0** **Миграция голосового канала на OpenAI Realtime API** — закрывает «push-to-talk без VAD/прерываний», «нет live-транскрипта», «долгая пауза после ответа», «P2 A2. Realtime-транскрипция», «P1 Режим непрерывного интервью». Что появилось:
  - **Backend**: новый `app/llm/realtime.py` (`RealtimeBridge` к `wss://api.openai.com/v1/realtime`, server-VAD `silence_duration_ms=500`, tool `submit_answer`); `_run_realtime` в `routers/interview_ws.py` — двунаправленный мост с параллельной evaluation через `asyncio.to_thread` (не блокирует голосовой поток). Legacy-пайплайн сохранён в `_run_legacy` и используется для text-сессий и при `VOICE_REALTIME_ENABLED=False`.
  - **Frontend**: `useVoiceSession` переписан под два режима (`mode: voice|text`); микрофон через `AudioWorkletNode` (PCM16 24 kHz, новый `pcmWorklet.js`); TTS-чанки склеиваются в очередь `AudioBufferSourceNode` встык; новые компоненты `MicLevelMeter` и `LiveTranscript`; barge-in (по VAD speech во время TTS — `cancel_response` в Realtime).
  - **Pricing**: `cost_tracker.REALTIME_PRICING_PER_M_TOKENS` (text $5/$20, audio $100/$200 за 1M токенов, кеш ×0.5); `record_realtime_usage` пишется на каждый `response.done`. `total_cost_usd` теперь отражает реальную стоимость.
  - **Промпт интервьюера** (`REALTIME_INTERVIEWER_SYSTEM`): тёплый наставник, наводящие подсказки при заминке, ответы на уточняющие вопросы кандидата без раскрытия ответа, чёткие правила «когда вызывать submit_answer» и «в transcript передавай только речь кандидата».
  - **Лог**: запись в журнал ответов делается ТОЛЬКО на `submit_answer` — уточнения и реплики-маркеры не засчитываются как ответы.
  - **Голоса**: список Realtime API — `alloy, ash, ballad, coral, echo, sage, shimmer, verse, marin, cedar`. Селекторы в админке и `ALLOWED_VOICES` обновлены; старые имена (`onyx/fable/nova`) откатываются на `alloy` через `resolve_realtime_voice`.
  - **Feature flag**: `VOICE_REALTIME_ENABLED=False` возвращает старый пайплайн (для отладки или если ключ без Realtime-доступа).
- [x] **P1** **STT падает при коротком/пустом аудио** — на фронте `useVoiceSession` теперь проверяет `MIN_RECORDING_MS=600` / `MIN_BLOB_BYTES=1500` и не отправляет пустышки; на бэке `llm/voice.py` ввёл `AudioTooShortError`, ловит его и `BadRequestError(audio_too_short)` от Whisper; WS возвращает `{"type":"error","code":"audio_too_short","recoverable":true}`, диалог не падает.
- [x] **P1** **Сырые тексты ошибок в голосовой панели** — `VoicePanel` показывает короткое сообщение в отдельной плашке с кнопкой «Закрыть»; ошибки от сервера несут `code/recoverable`, recoverable-ошибки автоматически возвращают phase в `listening`.
- [x] **P1** **Очевиднее управление микрофоном** — заменил hold-to-talk на круглую toggle-кнопку микрофона с пульсацией при записи и понятной подписью «Нажмите, чтобы начать запись» / «Идёт запись — нажмите ещё раз, чтобы отправить»; добавил цветной индикатор фазы.
- [x] **P1** **Просмотр и редактирование проектов** — добавил страницу `/requirements/:id` (`RequirementsDetail.tsx`) с summary/темами/банком вопросов и счётчиками 5/5 на пару. Эндпоинты на бэке: `GET/PATCH/DELETE /api/requirements/:id` и `POST /api/requirements/:id/regenerate`. Под-пункт «не начинать сразу» — закрыт ниже.
- [x] **P1** **Не начинать вопросы автоматически** — на странице `/sessions/:id/interview` показывается заглушка с кнопкой «Начать интервью»; до клика WS не подключается, кодинг-задача доступна сразу.
- [x] **Голос — заменить на более человечный** — в `.env` `OPENAI_TTS_VOICE` переключён `alloy → nova`.
- [x] **P1** **Слишком мало вопросов на пару тема×уровень** — ужесточил `EXTRACT_SYSTEM` (явное «РОВНО 5 вопросов на каждую пару, отклонения недопустимы»), добавил `TOPIC_QUESTIONS_SYSTEM`/схему и функцию `generate_questions_for_pair` в `llm/extract.py`. Новая `ensure_full_bank` догенерирует недостающие вопросы по любой неполной паре. Вызывается и при загрузке, и при `regenerate`.
- [x] **Лайв-кодинг по теме** — на сессию генерируются ровно 3 кодинг-задачи, каждая привязана к теме сессии (если тем < 3 — повторение, если > 3 — сэмплинг без повторов). `CODING_TASKS_SYSTEM`/схема + `generate_coding_tasks` в `llm/generate.py`; в `routers/sessions.py` добавлены `_pick_coding_topics`, `CODING_TASKS_PER_SESSION=3`. Эндпоинт ревью переехал на `POST /api/sessions/{sid}/coding/review/{item_id}` и берёт prompt из конкретного `SessionQuestion`. На фронте `CodingPanel` показывает вкладки `[ #1 Topic ] [ #2 Topic ] [ #3 Topic ]` с отдельным редактором, кодом, verdict-индикатором и результатом для каждой задачи.
- [x] **Базовое серверное логирование** — `app/logging_setup.py` (`setup_logging()`), `LOG_LEVEL` в `config.py`/.env.example, инициализация в `main.py`. Логи INFO в роутерах `requirements`/`sessions` (загрузка, regenerate, delete, create_session с topics/voice/coding, start, submit_coding, finish) и в LLM-модулях (`extract`, `generate`, `evaluate`, `voice`) — без полного payload, только размеры/числа/verdict. Уровень доступа uvicorn приглушён до WARNING.
- [x] **Кнопка «Отправить ответ» + многосегментная запись** — в `useVoiceSession` запись больше не отправляется автоматически на stop; toggle микро = старт/пауза, накопленные чанки копятся в `chunksRef`, новый метод `submitAnswer()` склеивает их и шлёт одним blob в WS. Появился `discardSegments()` и счётчик `segments` в state. В `VoicePanel` рядом с микро добавлены кнопки «Отправить ответ» (primary) и «Очистить» (secondary), под микрофоном — динамическая подпись и индикатор «Записано сегментов: N». Минимальные пороги длительности/размера применяются к сумме всех сегментов.
- [x] **P0** **B1. Эталонный ответ + разбор после оценки** — `evaluate_voice_answer` и `review_code` теперь возвращают `expected_answer` (эталон) и `explanation` (что упущено). Добавлены поля в `SessionQuestion` + миграция `0002_b1_expected_answer.py`. На фронте под каждым вопросом в `Report.tsx` — два новых блока: «Эталонный ответ» (зелёный) и «Что упущено» (жёлтый). Промпт `VOICE_EVAL_SYSTEM` усилен инструкцией писать развёрнутый образцовый ответ.
- [x] **P1** **A3. Таймер сессии 10–15 мин** — `Session.target_duration_min` (default 12) + миграция `0003_a3_session_timer.py`. WS отслеживает elapsed: за ≤2 мин до конца шлёт `time_warning`, при истечении — `done` с `reason="time_up"`. На странице `/sessions/:id/interview` — компонент `SessionTimer` с MM:SS и цветовой индикацией (emerald/amber/rose). В `VoicePanel` баннер для `time_warning` и сообщение «Время вышло».
- [x] **P1** **A4. Дашборд аналитики** — новый роутер `routers/analytics.py` с `GET /api/analytics/overview` (count sessions per level, средний score по темам, тренд за 30 дней, top-3 weak topics). На фронте — страница `pages/Analytics.tsx` с CSS-bar-чартами (без новых npm-deps). Маршрут `/analytics` в `App.tsx`, ссылка в `Layout.tsx`.
- [x] **P1** **A1. PDF-экспорт отчёта** — кнопка «Скачать PDF» на `Report.tsx`, бэкенд через WeasyPrint. Новый модуль `app/reports/pdf.py` с inline-CSS-шаблоном (А4, статистика, эталонные ответы, кодинг-ревью). Эндпоинт `GET /api/sessions/{id}/report.pdf`. Dockerfile дополнен `libpango/libcairo/libgdk-pixbuf` + `fonts-dejavu-core` для рендера кириллицы.
- [x] **P1** **B3. Adaptive follow-up** — `evaluate_voice_answer` теперь возвращает поле `follow_up_kind` (`easier`/`deeper`/`clarify`/null), промпт инструктирует упрощать при partial и углублять при correct (если есть глубина). В `interview_ws.py` снято ограничение «follow-up только при partial» — теперь разрешён и при correct (углубление); incorrect по-прежнему сразу переходит дальше.
- [x] **P1** **C1. Sandbox-исполнение кодинга** — кнопка «Запустить» в `CodingPanel` (для Python-задач). Новый модуль `app/sandbox/runner.py` запускает код в subprocess с `sys.executable -I -c`, timeout 5 сек, обрезает stdout/stderr до 16 KB. Эндпоинт `POST /api/sessions/{sid}/coding/run/{item_id}` возвращает `{exit_code, stdout, stderr, duration_ms, timed_out, truncated}`. На фронте — terminal-style вывод (зелёный stdout, красный stderr).
- [x] **P1** **C7. Учёт стоимости OpenAI per session** — таблица `llm_usage` (`session_id`, `requirements_id`, `kind`, `model`, токены, `cost_usd`) + миграция `0004_c7_llm_usage.py`. Новый модуль `app/llm/cost_tracker.py` с тарифами на 2026-Q1 (gpt-4o-mini, gpt-4o, tts-1, whisper-1). Все LLM-функции в `llm/{extract,generate,evaluate,voice}.py` принимают опциональные `session_id` / `requirements_id` и пишут usage. В `ReportOut` добавлено `total_cost_usd`, на фронте отображается в шапке отчёта.
- [x] **P1** **C4+C5. Resume сессии и WS reconnect** — на странице `/sessions/:id/interview` добавлен `useEffect`, который автоматически открывает `VoicePanel` если `session.status === "active"` (resume после refresh). В `useVoiceSession` авто-reconnect с backoff `[1s, 2s, 4s]`: при `ws.onclose` ставится таймер на повтор `connect()`. Если попытки исчерпаны — recoverable=false ошибка. В `VoicePanel` индикатор «Восстанавливаем соединение...» с пульсирующей точкой.
- [x] **P2** **C6. Кэш по hash требований** — поле `Requirements.content_hash` (sha256) + миграция `0005_p2_hash_paste.py`. В `upload_requirements` перед LLM-extract проверяем, есть ли у пользователя проект с таким же hash; если есть — клонируем `summary`/`topics`/`question_bank` без LLM-вызовов. Экономит токены на повторных загрузках одного и того же материала.
- [x] **P2** **C2. Anti-paste для кодинга** — поле `SessionQuestion.paste_chars` (та же миграция `0005`). В `CodingPanel` слушаем `editor.onDidPaste` Monaco и копим суммарную длину вставленного текста по каждой задаче. При submit отправляем `paste_chars` в `CodingReviewIn`; при ratio ≥50% к длине ответа — добавляем в `rationale` пометку `[Anti-paste]` для интервьюера.
- [x] **P2** **C3. Метрика уверенности голоса** — `transcribe_audio` переведён на `response_format="verbose_json"`, теперь возвращает `TranscriptionResult` с `VoiceMetrics` (audio_seconds, word_count, speaking_rate_wps, pauses_count по сегментам с порогом 0.7 сек). `confidence_hint()` формирует короткую характеристику («ровный темп», «медленно, с раздумьями», «очень быстро» и т.п.). В `evaluate_voice_answer` добавлен опц. параметр `voice_signals: str | None` — пробрасывается в user-промпт; `VOICE_EVAL_SYSTEM` инструктирует **не** оценивать знания по темпу, но учитывать его при выборе `follow_up_kind` (медленно → clarify/easier, быстро+поверхностно → deeper) и **не** цитировать признаки в `rationale`. WS-роутер пробрасывает hint из `stt_result.metrics`.
- [x] **Текстовый ответ + явная кнопка «К следующему»** — в WS добавлен `answer_text` (без STT, сразу evaluate) с порогом ≥5 символов. Логика evaluate/save/follow-up вынесена в общий `_evaluate_and_advance` helper в `interview_ws.py`, переиспользуется и для голоса, и для текста. На фронте: новый метод `useVoiceSession.submitTextAnswer(text)`, в `VoicePanel` появилась кнопка «Ответить текстом / кодом» (toggle), раскрывающая textarea с моноширинным шрифтом и кнопкой «Отправить текстом». Кнопка «Пропустить вопрос» переоформлена как заметная «К следующему вопросу →» с пунктирной рамкой. Полезно для вопросов, где голос неуместен (фрагменты кода, формулы, конфиги).
- [x] **UI при `time_up`: явный лейбл и скрытие кнопок** — раньше при истечении времени в `VoicePanel` отображалось «Голосовые вопросы пройдены», что путало (фактически отвечено N<M). Теперь при `phase==="done" && doneReason==="time_up"` — лейбл «Время вышло (отвечено N из M)» с красным индикатором. Блок управления (микрофон, «Отправить ответ», «Очистить», «Ответить текстом», «К следующему») при `phase==="done"` скрывается, единственный CTA — «Завершить досрочно» в шапке.
- [x] **Code-review фиксы (HIGH/MED)** — пакет улучшений по итогам критического ревью бэкенда и фронтенда:
  - Frontend `useVoiceSession.ts`: добавлен `safeSend()` (проверка `readyState===OPEN` перед всеми send'ами), `isMountedRef` (предотвращает reconnect/setState после unmount), try/catch вокруг `JSON.parse(ev.data)`. В обработчике `transcript` topic/text/isFollowUp берутся из `s.current` с проверкой `itemId` — устранён race с `pendingItemRef` при follow-up.
  - Frontend `Report.tsx`: PDF-download получил `catch` с понятными сообщениями (401/404/общая ошибка) и баннер «Закрыть».
  - Frontend `VoicePanel.tsx` / `CodingPanel.tsx`: textMode сбрасывается при `phase==="done"`; `pasteCharsById` / `runOutputById` сбрасываются при смене `session.id`.
  - Backend `app/sandbox/runner.py`: `preexec_fn=_apply_rlimits` на POSIX — `RLIMIT_AS=256 MB`, `RLIMIT_CPU=6 s`, `RLIMIT_FSIZE=1 MB`. На Windows тихо деградирует до timeout-only.
  - Backend `app/routers/interview_ws.py`: при `time_up` оставшиеся voice-вопросы помечаются `Verdict.skipped` с rationale «Не задано — время сессии вышло» — состояние сессии становится consistent независимо от того, нажмёт ли клиент `/finish`.
  - Backend `app/routers/requirements.py`: `content_hash` записывается только после успешного extract; cache-search фильтрует `summary != ""` — «битые» записи больше не залипают.
  - Backend `app/routers/sessions.py`: `paste_chars` clamp в `[0, len(code)]`, threshold поднят с 0.5 → 0.7 (меньше false-positives).
  - Backend `app/llm/cost_tracker.py`: TTS pricing fallback на `tts-1` для неизвестных моделей, docstring `_record()` фиксирует контракт «независимая транзакция, best-effort».
  - Backend `app/llm/voice.py`: warning-лог при `verbose_json` без segments.
